@page "/Experiment/AddConfiguration/{experimentId:int}"
@model Assignment1_PRN222_Group7.Pages.Experiment.AddConfigurationModel
@{
    ViewData["Title"] = "Add Configuration";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0"><i class="bi bi-plus-circle"></i> Add Configuration</h4>
                    <small>Experiment: @Model.ExperimentName</small>
                </div>
                <div class="card-body">
                    <form method="post">
                        <input type="hidden" asp-for="ConfigurationForm.ExperimentId" />

                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                        <div class="mb-3">
                            <label asp-for="ConfigurationForm.ConfigName" class="form-label fw-bold">Configuration Name *</label>
                            <input asp-for="ConfigurationForm.ConfigName" class="form-control" placeholder="e.g., RAG-Recursive-multilingual-e5" />
                            <span asp-validation-for="ConfigurationForm.ConfigName" class="text-danger"></span>
                        </div>

                        <hr />
                        <h6 class="text-muted mb-3">Chunking Settings</h6>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label asp-for="ConfigurationForm.ChunkingStrategy" class="form-label">Chunking Strategy</label>
                                <select asp-for="ConfigurationForm.ChunkingStrategy" class="form-select">
                                    <option value="Fixed">Fixed</option>
                                    <option value="Recursive">Recursive</option>
                                    <option value="Semantic">Semantic</option>
                                    <option value="Sentence">Sentence</option>
                                </select>
                            </div>
                            <div class="col-md-3 mb-3">
                                <label asp-for="ConfigurationForm.ChunkSize" class="form-label">Chunk Size</label>
                                <input asp-for="ConfigurationForm.ChunkSize" class="form-control" type="number" min="100" max="2000" />
                            </div>
                            <div class="col-md-3 mb-3">
                                <label asp-for="ConfigurationForm.ChunkOverlap" class="form-label">Overlap</label>
                                <input asp-for="ConfigurationForm.ChunkOverlap" class="form-control" type="number" min="0" max="500" />
                            </div>
                        </div>

                        <hr />
                        <h6 class="text-muted mb-3">Embedding & Retrieval Settings</h6>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label asp-for="ConfigurationForm.EmbeddingModel" class="form-label">Embedding Model</label>
                                <select asp-for="ConfigurationForm.EmbeddingModel" class="form-select">
                                    <option value="GeminiEmbedding004">Gemini Text Embedding 004 (Miễn phí)</option>
                                    <option value="MultilingualE5Base">Multilingual E5 Base (HuggingFace)</option>
                                    <option value="TextEmbedding3Small">Text Embedding 3 Small (OpenAI)</option>
                                    <option value="PhoBERTBase">PhoBERT Base (Vietnamese)</option>
                                    <option value="BgeM3">BGE M3 (BAAI)</option>
                                </select>
                            </div>
                            <div class="col-md-6 mb-3">
                                <label asp-for="ConfigurationForm.RetrievalMethod" class="form-label">Retrieval Method</label>
                                <select asp-for="ConfigurationForm.RetrievalMethod" class="form-select">
                                    <option value="RAG">RAG</option>
                                    <option value="FineTuned">Fine-tuned</option>
                                    <option value="Hybrid">Hybrid</option>
                                </select>
                            </div>
                        </div>

                        <hr />
                        <h6 class="text-muted mb-3">Search Settings</h6>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label asp-for="ConfigurationForm.TopK" class="form-label">Top K (Chunks to retrieve)</label>
                                <input asp-for="ConfigurationForm.TopK" class="form-control" type="number" min="1" max="20" />
                            </div>
                            <div class="col-md-6 mb-3">
                                <label asp-for="ConfigurationForm.SimilarityThreshold" class="form-label">Similarity Threshold</label>
                                <input asp-for="ConfigurationForm.SimilarityThreshold" class="form-control" type="number" step="0.01" min="0" max="1" />
                            </div>
                        </div>

                        <div class="d-flex justify-content-between mt-4">
                            <a asp-page="/Experiment/Details" asp-route-id="@Model.ConfigurationForm.ExperimentId" class="btn btn-secondary">
                                <i class="bi bi-arrow-left"></i> Back
                            </a>
                            <button type="submit" class="btn btn-primary">
                                <i class="bi bi-save"></i> Add Configuration
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
